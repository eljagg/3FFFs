# v25.7.0.4.5 — Page-content-width (the OM-avatar-alignment line)

I was over-engineering the previous attempts. You wanted the cream dashboard to fill the page's CONTENT width — same width as the rest of the page, aligning with where the "OM" avatar sits in the header. NOT viewport-bleed.

## What changed

`tacticBodyWideViz` no longer uses position+transform or margin-calc tricks. It's just `width: 100%` — fills its parent (the Page wrapper at 1100px max). Renders as a sibling of `tacticBody` so it isn't squeezed by `tacticBody`'s nested 84/40 padding.

`PositioningTwoViews` outer stage has its border + border-radius restored, since the component now renders at page-content-width where rounded corners look fine.

`Page.jsx` reverted to its original form (no longer needs the outer overflow wrapper — without bleed, no scrollbar issues).

Techniques collapse toggle from .4 carries forward unchanged.

## Files changed

- `client/src/pages/Framework.jsx` — wide-viz now uses simple width:100% inside Page wrapper, same parent as tacticBody
- `client/src/components/visualizations/PositioningTwoViews.jsx` — restored stage border + radius

`Page.jsx` is NOT in this zip — reverting to the project's original.

## Deploy

```bash
cd /workspaces/3FFFs
unzip -o v25.7.0.4.5-3FFFs.zip
rm v25.7.0.4.5-3FFFs.zip
git add -A
git status
git commit -m "v25.7.0.4.5: cream dashboard fills page content width (OM-alignment)"
git push origin main
```

You may want to first revert any local changes to `client/src/components/Page.jsx` from previous patches — `git checkout client/src/components/Page.jsx` will reset it to the version on main.

## Verification

After deploy, expand Positioning. Expected:

- Cream dashboard fills the page content width — left edge of cream aligns with the left edge of the page nav/heading; right edge of the dark threat panel aligns with the right edge of the OM avatar
- Allison's full record visible (name, ID, risk badge, quick stats, full data tables) without clipping
- Money mule + Hidden signals + Detection controls in the right column
- Techniques collapsed by default — click "25 techniques and sub-techniques →" to expand
